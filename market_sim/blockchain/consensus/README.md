Implementation: Rust crate at repo root `blockchain_consensus_rust/`.

Build & test: `cd ../../../blockchain_consensus_rust && cargo build && cargo test`

Run: `cargo run` (chain growth JSON), `cargo run -- dolev` (Dolev-Strong).
